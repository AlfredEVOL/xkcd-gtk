#!/usr/bin/make -f

%:
	dh $@ 

override_dh_auto_build:
	cd $(PACKAGE_ROOT) && make

override_dh_auto_test:
	cd $(PACKAGE_ROOT) && make check
	# Houston CI doesn't support the `-race` flag, so unset it.
	cd $(PACKAGE_ROOT) && make test DEVFLAGS=
